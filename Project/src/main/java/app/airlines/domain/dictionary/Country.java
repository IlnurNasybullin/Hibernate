package app.airlines.domain.dictionary;

import app.airlines.domain.AbstractDictionary;

import javax.persistence.Entity;

@Entity(name = "dict_country")
public class Country extends AbstractDictionary { }
